# Label Audit: annotation-derived vs bucket folder

- Total labeled images: **523** (safe 272 / unsafe 251).
- Labels are ground truth from **annotations** (a box labelled `unsafe` ⇒ unsafe; else `safe`; `license` ignored), which can differ from the R2 bucket folder name.
- **0 image(s)** have an annotation label that disagrees with their folder.

_No folder/annotation disagreements in the current set._
